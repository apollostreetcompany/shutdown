{
  "name": "shutdown-assistant",
  "type": "module",
  "version": "1.0.0",
  "description": "The most comprehensive US business shutdown directory - 50 states, every form, every fee, every step.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name shutdownassistant",
    "astro": "astro",
    "generate:registry": "node scripts/generate-registry.mjs",
    "generate:guides": "node scripts/generate-guides.mjs",
    "validate": "node scripts/validate.mjs"
  },
  "dependencies": {
    "@astrojs/react": "^4.2.0",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^5.7.0",
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.7.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^3.4.0"
  }
}
